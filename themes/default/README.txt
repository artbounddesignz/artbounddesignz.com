This is the default theme directory.  It contains mustache templates and any
custom resource files like javascript, css, images, etc.

Chill by default looks for a base.mustache and builds the page from
there.
